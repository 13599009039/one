# 企业资源计划管理系统(ERP) - 系统说明文档

**版本**: 19.1  
**更新日期**: 2026年2月11日  
**公司**: 许昌爱佳网络科技有限公司  
**系统定位**: 企业资源计划管理系统(ERP) + SaaS多租户模式

### 版本记录

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| v19.1 | 2026-02-11 | **前端重构评估完成**：架构分析+拆分方案+回滚策略+框架选型+迁移计划（评估报告） |
| v19.0 | 2026-02-11 | **日期筛选功能完成**：4个API增加日期筛选（财务流水/库存流水/订单/成本日志）+新增成本日志查询API |
| v18.0 | 2026-02-11 | **权限系统完成**：RBAC权限模型+装饰器+7个关键API权限验证（退款/库存/成本） |
| v17.1 | 2026-02-11 | **进度报告完善**：创建P2/P3_PROGRESS_REPORT.md，完整记录三阶段优化过程 |
| v17.0 | 2026-02-11 | **三阶段优化全部完成**：P1财务流水+P2库存成本+任务成本API（后端100%完成） |
| v16.1 | 2026-02-11 | **P2阶段最终完成**：任务成本管理API完善（4个接口+变更日志） |
| v16.0 | 2026-02-11 | **P2阶段完成**：价格历史记录、库存管理（后端+API完成，前端暂缓） |
| v15.1 | 2026-02-11 | **P1阶段完成**：财务流水与订单关联、退款流程（后端+API测试100%完成，前端UI暂缓） |
| v15.0 | 2026-02-11 | **P1阶段优化**：财务流水与订单关联、退款流程完善（数据库+API完成） |
| v14.2 | 2026-02-11 | 完成业务流程测试，生成测试计划和测试报告，发现8个待优化问题 |
| v14.1 | 2026-02-11 | 完善开发文档、验证功能实现、同步代码与文档 |
| v14.0 | 2026-02-09 | 完整系统说明文档，覆盖所有功能模块 |

---

## 📚 目录

1. [系统概述](#系统概述)
2. [技术架构](#技术架构)
3. [功能模块说明](#功能模块说明)
4. [业务流程](#业务流程)
5. [数据库设计](#数据库设计)
6. [API接口文档](#api接口文档)
7. [部署与运维](#部署与运维)
8. [使用指南](#使用指南)
9. [常见问题](#常见问题)

---

## 系统概述

### 系统简介

**企业资源计划管理系统(ERP)**是一套面向多企业的SaaS化综合业务管理平台，集成了客户主数据管理(MDM)、订单管理、商品服务管理、财务流水、任务派发、组织架构管理等多个核心ERP功能模块。系统采用**多租户架构**，支持通过超级控制台统一管理多家企业客户，实现从客户跟进、订单签约、任务执行到财务结算的全流程数字化管理。

**产品定位**:
- **主系统**: 单租户ERP系统(`financial_system.html`)，面向具体企业的日常业务操作
- **超级控制台**: SaaS管理平台(`console.html`)，面向平台运营方，统一管理所有企业客户

### 核心价值

- **全流程管理**: 覆盖客户获取、订单签约、任务执行、财务结算完整业务链条
- **多公司支持**: 支持一个系统管理多家公司的业务数据
- **权限管理**: 基于角色的权限控制，数据隔离安全可靠
- **实时统计**: 多维度数据统计分析，辅助经营决策
- **移动适配**: 响应式设计，支持PC端和移动端访问

### 适用场景

- 网络服务公司（短视频拍摄、直播代运营、广告投放等）
- 财务代理记账公司
- 需要客户管理和订单管理的中小企业

---

## 技术架构

### 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| HTML5 | - | 页面结构 |
| JavaScript | ES6+ | 业务逻辑 |
| Tailwind CSS | 2.2.19 | UI样式框架 |
| Font Awesome | 6.0.0 | 图标库 |
| Chart.js | 3.7.0 | 数据可视化 |

### 后端技术栈（新）

| 技术 | 版本 | 用途 |
|------|------|------|
| Python | 3.12 | 后端语言 |
| Flask | 2.3.3 | Web框架 |
| PyMySQL | 1.1.0 | MySQL数据库驱动 |
| Flask-CORS | 4.0.0 | 跨域资源共享 |
| Gunicorn | 21.2.0 | WSGI服务器 |

### 数据库

- **MySQL 8.0.36**: 关系型数据库
- **LocalStorage**: 浏览器本地缓存（旧版架构，正在迁移）

### 服务器环境

- **Web服务器**: Nginx
- **操作系统**: Linux Ubuntu 24.04
- **宝塔面板**: 服务器管理工具

### 架构演进

#### 旧架构（v1.0 - v4.5）
```
浏览器 → HTML/JavaScript → LocalStorage
```
- **优点**: 部署简单、无需后端
- **缺点**: 数据仅存本地、无法多设备同步、容量限制

#### 新架构（v4.6+）
```
浏览器 → HTML/JavaScript → Flask API → MySQL数据库
```
- **优点**: 数据永久存储、多设备同步、多用户协作
- **缺点**: 需要服务器和数据库支持

---

## 功能模块说明

### 1. 登录认证模块 (login.js)

**功能描述**:
- 用户身份验证
- 登录状态持久化（LocalStorage）
- 自动登录检测

**业务逻辑**:
1. 用户输入用户名和密码
2. 系统调用 `db.login()` 验证身份
3. 验证通过后保存登录状态到 LocalStorage
4. 跳转到系统主页面（仪表盘）
5. 初始化用户权限和公司信息

**关键函数**:
- `initLoginForm()`: 初始化登录表单
- `checkLoginStatus()`: 检查登录状态
- `logout()`: 退出登录

### 2. 数据库模块 (database.js)

**功能描述**:
- 数据持久化（LocalStorage）
- 提供统一的数据访问接口
- 初始化默认数据（7,824条商家+41名人员）

**核心数据实体**:

| 实体 | 说明 | 主要字段 |
|------|------|---------|
| users | 用户/人员 | id, username, password, name, role, company_id |
| customers | 客户/商家 | id, merchant_id, shop_name, industry, status, follower_id |
| orders | 订单 | id, customer_id, order_date, contract_amount, status |
| transactions | 财务流水 | id, transaction_date, amount, payer_name, payee_name |
| task_pool | 任务池 | id, order_id, status, assigned_user_id |
| companies | 公司 | id, name, contact_person, service_fee |
| accounts | 账户 | id, name, bank_name, balance |

**关键函数**:
- `getDefaultData()`: 获取默认数据模板
- `saveData()`: 保存数据到LocalStorage
- `login(username, password)`: 用户登录
- `getAllCustomers()`: 获取所有客户
- `addCustomer(customer)`: 添加客户
- `updateCustomer(id, customer)`: 更新客户
- `deleteCustomer(id)`: 删除客户

### 3. 客户管理模块 (customers.js)

**功能描述**:
- 客户信息全生命周期管理
- 支持分页展示（默认20条/页）
- 搜索和筛选功能
- 多联系人和备忘录管理

**业务场景**:
1. **新增客户**: 录入商家基本信息、联系人、合作模式等
2. **客户跟进**: 记录沟通备忘、更新跟进状态
3. **客户分配**: 将客户分配给指定跟进人员
4. **客户转化**: 跟进中 → 合作中 → 已签约

**数据字段**（35个字段）:

| 字段类别 | 字段名 | 说明 |
|---------|--------|------|
| 基本信息 | merchant_id | 商家ID（抖音ID） |
| | shop_name | 店铺名称 |
| | douyin_name | 抖音号 |
| | company_name | 公司名称 |
| | credit_code | 统一社会信用代码 |
| | legal_person | 法人 |
| 业务信息 | cooperation_mode | 合作模式（短视频拍摄/直播代运营等） |
| | industry | 所属行业 |
| | category | 类目 |
| | status | 状态（跟进中/合作中/已流失） |
| 人员分配 | follower_id | 跟进人 |
| | business_staff | 商务人员 |
| | service_staff | 服务人员 |
| | operation_staff | 运营人员 |
| 组织架构 | team | 所属团队 |
| | region | 所属地区 |
| | project | 所属项目 |
| | company | 所属公司 |
| 扩展信息 | contacts | 联系人列表（姓名、电话、职位） |
| | memos | 备忘录列表（日期、类型、内容） |
| | tags | 标签 |

**关键函数**:
- `loadCustomersData()`: 加载客户列表（支持分页）
- `getFilteredCustomers()`: 获取过滤后的客户
- `openCustomerModal(customerId)`: 打开客户编辑弹窗
- `saveCustomer()`: 保存客户信息
- `deleteCustomer(customerId)`: 删除客户
- `changeCustomerPage(page)`: 切换页码
- `handleCustomerSearch()`: 搜索处理（300ms防抖）

**分页机制**:
```javascript
// 分页变量
let customerPageSize = 20;  // 每页显示数量（可选20/50/100/500/全部）
let customerCurrentPage = 1;  // 当前页码

// 计算分页数据
const start = (currentPage - 1) * pageSize;
const end = start + pageSize;
const paginatedCustomers = allCustomers.slice(start, end);
```

### 4. 订单管理模块 (orders.js)

**功能描述**:
- 订单创建与编辑
- 多商品/服务项支持
- 收款记录管理
- 售后管理

**订单生命周期**:
```
签约 → 制作中 → 待验收 → 已完成 → 售后（可选）
```

**核心字段**:
- **基本信息**: 订单日期、客户、合同金额
- **人员配置**: 商务、服务、运营、管理人员
- **组织信息**: 团队、地区、项目、公司
- **商品项**: 服务名称、类型、单价、数量、小计
- **收款记录**: 收款日期、金额、方式、备注
- **售后记录**: 售后时间、类型、描述、处理结果

**关键函数**:
- `openAddOrderModal()`: 打开新增订单弹窗
- `saveNewOrder()`: 保存订单
- `loadOrdersData()`: 加载订单列表
- `addOrderPayment(orderId)`: 添加收款记录
- `addAfterSales(orderId)`: 添加售后记录

### 5. 任务池模块 (taskpool.js)

**功能描述**:
- 将订单转化为可分配的任务
- 任务接单与派发机制
- 任务成本登记
- 任务状态跟踪

**任务状态流转**:
```
待接单 → 进行中 → 已完成
   ↓         ↓         ↓
 接单操作  成本登记  查看成本
```

**业务流程**:
1. **订单生成任务**: 订单创建后自动在任务池生成任务
2. **任务接单**: 运营人员从任务池接单（或管理员指派）
3. **成本登记**: 执行过程中登记各项成本
   - 拍摄费
   - 投放费
   - 人员成本
   - 设备租赁
   - 其他
4. **任务完成**: 标记任务完成，计算利润

**利润计算**:
```
利润 = 订单合同金额 - Σ任务成本
```

**关键函数**:
- `renderTaskPool()`: 渲染任务列表
- `handleAcceptTask(taskId)`: 接单
- `openTaskCostModal(taskId)`: 打开成本登记弹窗
- `saveTaskCost()`: 保存成本
- `handleCompleteTask(taskId)`: 完成任务

### 6. 财务流水模块 (transactions.js)

**功能描述**:
- 银行流水记录管理
- 收支分类统计
- 多账户管理
- 流水审核与作废

**流水类型**:
- **来账**: 收入类流水
- **往账**: 支出类流水

**核心功能**:
1. **流水录入**: 手动录入银行流水
2. **批量导入**: 支持从CSV文件导入（未实现）
3. **分类管理**: 为流水分配收支类别
4. **审核机制**: 流水审核状态管理
5. **账户余额**: 自动计算账户余额

**关键字段**:
- `transaction_date`: 交易日期
- `transaction_type`: 交易类型（来账/往账）
- `payer_bank`: 付款方银行
- `payer_name`: 付款方名称
- `payee_bank`: 收款方银行
- `payee_name`: 收款方名称
- `amount`: 金额
- `balance_after`: 交易后余额
- `purpose`: 用途
- `audit_status`: 审核状态

### 7. 组织架构模块 (organization.js)

**功能描述**:
- 人员信息管理
- 角色权限分配
- 部门/团队管理
- 公司管理

**用户角色**:
| 角色 | 权限 | 说明 |
|------|------|------|
| super_admin | 全部 | 超级管理员，可管理所有公司 |
| admin | 全部（本公司） | 公司管理员 |
| financial | 财务 | 财务人员，查看财务数据 |
| operation | 业务 | 运营人员，管理订单和任务 |
| viewer | 只读 | 只能查看数据 |

**关键函数**:
- `loadOrganizationData()`: 加载人员列表
- `openUserModal(userId)`: 打开人员编辑弹窗
- `saveUser()`: 保存人员信息
- `deleteUser(userId)`: 删除人员

### 8. 仪表盘模块 (dashboard.js)

**功能描述**:
- 核心业务数据概览
- 可视化图表展示
- 快捷操作入口

**统计维度**:
1. **客户统计**: 总客户数、合作中、跟进中、已流失
2. **订单统计**: 总订单数、进行中、已完成、本月新增
3. **财务统计**: 本月收入、本月支出、账户余额
4. **任务统计**: 待接单、进行中、已完成

**可视化图表**:
- 月度收支趋势图（折线图）
- 客户行业分布（饼图）
- 订单状态分布（柱状图）

### 9. 报表模块 (reports.js)

**功能描述**:
- 财务报表生成
- 业务数据分析
- 导出功能（Excel/PDF）

**报表类型**:
1. **财务报表**:
   - 收支明细表
   - 利润表
   - 资产负债表
2. **业务报表**:
   - 客户统计报表
   - 订单统计报表
   - 人员业绩报表

### 10. 系统设置模块 (settings.js)

**功能描述**:
- 系统参数配置
- 数据字典管理
- 数据导入导出

**配置项**:
- 系统名称
- 公司信息
- 合作模式列表
- 行业列表
- 收支类别

---

## 业务流程

### 完整业务流程图

```
┌─────────────┐
│  客户获取   │ → 市场拓展/商务洽谈
└──────┬──────┘
       ↓
┌─────────────┐
│  客户跟进   │ → 录入客户信息、分配跟进人、记录沟通备忘
└──────┬──────┘
       ↓
┌─────────────┐
│  签约订单   │ → 创建订单、填写合同金额、配置服务项
└──────┬──────┘
       ↓
┌─────────────┐
│ 任务生成    │ → 订单自动生成任务到任务池
└──────┬──────┘
       ↓
┌─────────────┐
│ 任务接单    │ → 运营人员从任务池接单
└──────┬──────┘
       ↓
┌─────────────┐
│ 任务执行    │ → 拍摄制作、广告投放、登记成本
└──────┬──────┘
       ↓
┌─────────────┐
│ 任务完成    │ → 标记任务完成、计算利润
└──────┬──────┘
       ↓
┌─────────────┐
│ 财务结算    │ → 客户付款、录入收款流水、更新账户余额
└──────┬──────┘
       ↓
┌─────────────┐
│ 售后服务    │ → 记录售后问题、处理结果（可选）
└─────────────┘
```

### 典型业务场景

#### 场景1: 新客户签约全流程

1. **商务人员**在"客户管理"新增客户
   - 填写商家名称: "XX餐厅"
   - 抖音ID: "123456789"
   - 行业: "餐饮"
   - 合作模式: "短视频拍摄"
   - 跟进人: "张三"
   - 状态: "跟进中"

2. **沟通备忘**记录
   - 2026-02-01: 初次接触，客户有意向
   - 2026-02-05: 发送报价单，等待反馈
   - 2026-02-08: 客户确认合作，准备签约

3. **更新客户状态**: 跟进中 → 合作中

4. **创建订单**
   - 订单日期: 2026-02-08
   - 客户: XX餐厅
   - 商务人员: 张三
   - 服务人员: 李四
   - 运营人员: 王五
   - 服务项:
     - 短视频拍摄 | 基础套餐 | ¥3,000 × 1
     - 账号代运营 | 月度套餐 | ¥2,000 × 1
   - 合同金额: ¥5,000
   - 备注: 月度服务，首月优惠

5. **系统自动生成任务**到任务池
   - 任务标题: XX餐厅-短视频拍摄项目
   - 关联订单: ORD202602080001
   - 状态: 待接单

6. **运营人员王五接单**
   - 任务状态: 待接单 → 进行中

7. **执行过程中登记成本**
   - 拍摄费: ¥1,000（外请摄影师）
   - 达人车马: ¥500（请达人到店拍摄）
   - 广告投放: ¥1,500（DOU+投放）
   - 总成本: ¥3,000

8. **任务完成**
   - 标记任务完成
   - 系统计算利润: ¥5,000 - ¥3,000 = ¥2,000

9. **客户付款**
   - 录入财务流水
   - 交易日期: 2026-02-20
   - 类型: 来账
   - 付款方: XX餐厅
   - 收款方: 许昌爱佳网络科技有限公司
   - 金额: ¥5,000
   - 用途: 短视频制作服务费
   - 账户余额自动更新

#### 场景2: 数据统计与分析

**管理人员**登录系统后：

1. **仪表盘查看**本月经营概况
   - 本月新增客户: 15家
   - 本月订单数: 23单
   - 本月收入: ¥158,000
   - 本月支出: ¥95,000
   - 本月利润: ¥63,000

2. **客户管理页面**查看客户统计
   - 总客户数: 7,824家
   - 合作中: 186家
   - 跟进中: 425家
   - 已流失: 103家

3. **任务池查看**任务进度
   - 待接单: 5个
   - 进行中: 12个
   - 已完成: 158个

4. **报表模块**导出月度报表
   - 选择日期: 2026年2月
   - 导出客户统计报表
   - 导出订单明细报表
   - 导出财务收支报表

---

## 数据库设计

### 数据库连接信息

```
数据库地址: 47.98.60.197
数据库名: ajkuaiji
用户名: ajkuaiji
密码: @HNzb5z75b16
端口: 3306 (默认)
字符集: utf8mb4
```

### 表结构设计

#### 1. users（用户表）

| 字段 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | INT | 用户ID | PRIMARY KEY, AUTO_INCREMENT |
| username | VARCHAR(50) | 登录用户名 | UNIQUE, NOT NULL |
| password | VARCHAR(255) | 登录密码 | NOT NULL |
| name | VARCHAR(100) | 真实姓名 | NOT NULL |
| alias | VARCHAR(100) | 别名/花名 | - |
| role | VARCHAR(50) | 角色 | NOT NULL |
| company_id | INT | 所属公司ID | - |
| status | VARCHAR(20) | 状态 | DEFAULT 'enabled' |
| created_at | TIMESTAMP | 创建时间 | DEFAULT CURRENT_TIMESTAMP |
| updated_at | TIMESTAMP | 更新时间 | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP |

**索引**:
- `idx_username` (username)
- `idx_company_id` (company_id)

#### 2. customers（客户表）

| 字段 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | INT | 客户ID | PRIMARY KEY, AUTO_INCREMENT |
| merchant_id | VARCHAR(100) | 商家ID（抖音ID） | UNIQUE |
| shop_name | VARCHAR(200) | 店铺名称 | NOT NULL |
| douyin_name | VARCHAR(200) | 抖音号 | - |
| company_name | VARCHAR(200) | 公司名称 | - |
| credit_code | VARCHAR(100) | 统一社会信用代码 | - |
| legal_person | VARCHAR(100) | 法人 | - |
| registered_capital | VARCHAR(100) | 注册资本 | - |
| business_address | TEXT | 注册地址 | - |
| operating_address | TEXT | 经营地址 | - |
| cooperation_mode | VARCHAR(100) | 合作模式 | - |
| category | VARCHAR(100) | 类目 | - |
| industry | VARCHAR(100) | 行业 | - |
| status | VARCHAR(50) | 状态 | DEFAULT '跟进中' |
| follower_id | INT | 跟进人ID | - |
| business_staff | VARCHAR(100) | 商务人员 | - |
| service_staff | VARCHAR(100) | 服务人员 | - |
| operation_staff | VARCHAR(100) | 运营人员 | - |
| management_staff | VARCHAR(100) | 管理人员 | - |
| team | VARCHAR(100) | 团队 | - |
| region | VARCHAR(100) | 地区 | - |
| project | VARCHAR(100) | 项目 | - |
| company | VARCHAR(200) | 公司 | - |
| tags | TEXT | 标签（JSON数组） | - |
| created_at | TIMESTAMP | 创建时间 | DEFAULT CURRENT_TIMESTAMP |
| updated_at | TIMESTAMP | 更新时间 | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP |

**索引**:
- `idx_merchant_id` (merchant_id)
- `idx_shop_name` (shop_name)
- `idx_follower_id` (follower_id)
- `idx_status` (status)

#### 3. customer_contacts（客户联系人表）

| 字段 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | INT | 联系人ID | PRIMARY KEY, AUTO_INCREMENT |
| customer_id | INT | 客户ID | NOT NULL, FOREIGN KEY |
| name | VARCHAR(100) | 姓名 | - |
| phone | VARCHAR(50) | 电话 | - |
| position | VARCHAR(100) | 职位 | - |
| created_at | TIMESTAMP | 创建时间 | DEFAULT CURRENT_TIMESTAMP |

**外键**:
- FOREIGN KEY (customer_id) REFERENCES customers(id) ON DELETE CASCADE

#### 4. customer_memos（客户备忘录表）

| 字段 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | INT | 备忘录ID | PRIMARY KEY, AUTO_INCREMENT |
| customer_id | INT | 客户ID | NOT NULL, FOREIGN KEY |
| date | DATE | 日期 | - |
| type | VARCHAR(50) | 类型 | - |
| content | TEXT | 内容 | - |
| created_at | TIMESTAMP | 创建时间 | DEFAULT CURRENT_TIMESTAMP |

**外键**:
- FOREIGN KEY (customer_id) REFERENCES customers(id) ON DELETE CASCADE

#### 5. orders（订单表）

| 字段 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | INT | 订单ID | PRIMARY KEY, AUTO_INCREMENT |
| customer_id | INT | 客户ID | FOREIGN KEY |
| order_date | DATE | 订单日期 | - |
| business_staff | VARCHAR(100) | 商务人员 | - |
| service_staff | VARCHAR(100) | 服务人员 | - |
| operation_staff | VARCHAR(100) | 运营人员 | - |
| management_staff | VARCHAR(100) | 管理人员 | - |
| team | VARCHAR(100) | 团队 | - |
| region | VARCHAR(100) | 地区 | - |
| project | VARCHAR(100) | 项目 | - |
| company | VARCHAR(200) | 公司 | - |
| contract_amount | DECIMAL(15,2) | 合同金额 | DEFAULT 0 |
| status | VARCHAR(50) | 状态 | DEFAULT '进行中' |
| remarks | TEXT | 备注 | - |
| created_at | TIMESTAMP | 创建时间 | DEFAULT CURRENT_TIMESTAMP |
| updated_at | TIMESTAMP | 更新时间 | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP |

**索引**:
- `idx_customer_id` (customer_id)
- `idx_order_date` (order_date)
- `idx_status` (status)

#### 6. order_items（订单明细表）

| 字段 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | INT | 明细ID | PRIMARY KEY, AUTO_INCREMENT |
| order_id | INT | 订单ID | NOT NULL, FOREIGN KEY |
| service_name | VARCHAR(200) | 服务名称 | - |
| service_type | VARCHAR(100) | 服务类型 | - |
| price | DECIMAL(15,2) | 单价 | DEFAULT 0 |
| quantity | INT | 数量 | DEFAULT 1 |
| total | DECIMAL(15,2) | 小计 | DEFAULT 0 |

**外键**:
- FOREIGN KEY (order_id) REFERENCES orders(id) ON DELETE CASCADE

#### 7. task_pool（任务池表）

| 字段 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | INT | 任务ID | PRIMARY KEY, AUTO_INCREMENT |
| order_id | INT | 订单ID | NOT NULL, FOREIGN KEY |
| status | VARCHAR(50) | 状态 | DEFAULT '待接单' |
| assigned_user_id | INT | 执行人ID | FOREIGN KEY |
| accepted_at | TIMESTAMP | 接单时间 | NULL |
| completed_at | TIMESTAMP | 完成时间 | NULL |
| created_at | TIMESTAMP | 创建时间 | DEFAULT CURRENT_TIMESTAMP |
| updated_at | TIMESTAMP | 更新时间 | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP |

**索引**:
- `idx_order_id` (order_id)
- `idx_status` (status)

#### 8. cost_categories（成本类别表）

| 字段 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | INT | 类别ID | PRIMARY KEY, AUTO_INCREMENT |
| name | VARCHAR(100) | 类别名称 | NOT NULL |
| description | TEXT | 描述 | - |
| created_at | TIMESTAMP | 创建时间 | DEFAULT CURRENT_TIMESTAMP |

**预设类别**:
- 拍摄费
- 投放费
- 人员成本
- 设备租赁
- 其他

#### 9. task_costs（任务成本表）

| 字段 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | INT | 成本ID | PRIMARY KEY, AUTO_INCREMENT |
| task_id | INT | 任务ID | NOT NULL, FOREIGN KEY |
| category_id | INT | 类别ID | FOREIGN KEY |
| amount | DECIMAL(15,2) | 金额 | DEFAULT 0 |
| remark | TEXT | 备注 | - |
| created_at | TIMESTAMP | 创建时间 | DEFAULT CURRENT_TIMESTAMP |

**外键**:
- FOREIGN KEY (task_id) REFERENCES task_pool(id) ON DELETE CASCADE
- FOREIGN KEY (category_id) REFERENCES cost_categories(id) ON DELETE SET NULL

#### 10. transactions（财务流水表）

| 字段 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | INT | 流水ID | PRIMARY KEY, AUTO_INCREMENT |
| transaction_type | VARCHAR(50) | 交易类型 | NOT NULL |
| transaction_date | DATE | 交易日期 | NOT NULL |
| payer_bank | VARCHAR(200) | 付款方银行 | - |
| payer_name | VARCHAR(200) | 付款方名称 | - |
| payee_bank | VARCHAR(200) | 收款方银行 | - |
| payee_name | VARCHAR(200) | 收款方名称 | - |
| amount | DECIMAL(15,2) | 金额 | NOT NULL |
| balance_after | DECIMAL(15,2) | 交易后余额 | - |
| purpose | VARCHAR(200) | 用途 | - |
| remark | TEXT | 备注 | - |
| account_id | INT | 账户ID | - |
| company_id | INT | 公司ID | - |
| created_by | INT | 创建人ID | - |
| audit_status | VARCHAR(50) | 审核状态 | DEFAULT '待审核' |
| is_void | TINYINT | 是否作废 | DEFAULT 0 |
| created_at | TIMESTAMP | 创建时间 | DEFAULT CURRENT_TIMESTAMP |
| updated_at | TIMESTAMP | 更新时间 | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP |

**索引**:
- `idx_transaction_date` (transaction_date)
- `idx_transaction_type` (transaction_type)
- `idx_company_id` (company_id)

#### 11. companies（公司表）

| 字段 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | INT | 公司ID | PRIMARY KEY, AUTO_INCREMENT |
| name | VARCHAR(200) | 公司名称 | NOT NULL |
| short_name | VARCHAR(100) | 简称 | - |
| contact_person | VARCHAR(100) | 联系人 | - |
| contact_phone | VARCHAR(50) | 联系电话 | - |
| status | VARCHAR(50) | 状态 | DEFAULT 'active' |
| service_start_date | DATE | 服务开始日期 | - |
| service_fee | DECIMAL(15,2) | 服务费 | - |
| service_cycle | INT | 服务周期（月） | - |
| service_end_date | DATE | 服务结束日期 | - |
| service_status | VARCHAR(50) | 服务状态 | - |
| created_at | TIMESTAMP | 创建时间 | DEFAULT CURRENT_TIMESTAMP |

**索引**:
- `idx_name` (name)

#### 12. accounts（账户表）

| 字段 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | INT | 账户ID | PRIMARY KEY, AUTO_INCREMENT |
| name | VARCHAR(200) | 账户名称 | NOT NULL |
| company_id | INT | 公司ID | FOREIGN KEY |
| bank_name | VARCHAR(200) | 银行名称 | - |
| account_number | VARCHAR(100) | 账号 | - |
| balance | DECIMAL(15,2) | 余额 | DEFAULT 0 |
| initial_balance | DECIMAL(15,2) | 初始余额 | DEFAULT 0 |
| account_type | VARCHAR(50) | 账户类型 | - |
| status | VARCHAR(50) | 状态 | DEFAULT 'active' |
| created_at | TIMESTAMP | 创建时间 | DEFAULT CURRENT_TIMESTAMP |

**外键**:
- FOREIGN KEY (company_id) REFERENCES companies(id) ON DELETE SET NULL

#### 13. system_settings（系统设置表）

| 字段 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | INT | 设置ID | PRIMARY KEY, AUTO_INCREMENT |
| setting_key | VARCHAR(100) | 配置键 | UNIQUE, NOT NULL |
| setting_value | TEXT | 配置值 | - |
| description | VARCHAR(500) | 描述 | - |
| updated_at | TIMESTAMP | 更新时间 | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP |

**预设配置**:
- system_name: 系统名称
- system_version: 系统版本
- company_name: 公司名称
- cooperation_modes: 合作模式列表（JSON）
- industries: 行业列表（JSON）

### 数据库ER图（实体关系）

```
users (1) ─────────> (n) customers (跟进人)
users (1) ─────────> (n) task_pool (执行人)
customers (1) ──────> (n) orders
customers (1) ──────> (n) customer_contacts
customers (1) ──────> (n) customer_memos
orders (1) ─────────> (n) order_items
orders (1) ─────────> (1) task_pool
task_pool (1) ──────> (n) task_costs
cost_categories (1) ─> (n) task_costs
companies (1) ──────> (n) users
companies (1) ──────> (n) accounts
companies (1) ──────> (n) transactions
accounts (1) ───────> (n) transactions
```

---

## API接口文档

### 基础信息

**API地址**: `http://47.98.60.197:5000/api`  
**数据格式**: JSON  
**字符编码**: UTF-8

### 通用响应格式

#### 成功响应
```json
{
  "success": true,
  "data": {...},
  "message": "操作成功"
}
```

#### 失败响应
```json
{
  "success": false,
  "message": "错误信息"
}
```

### 接口列表

#### 1. 健康检查

**接口**: `GET /api/health`  
**说明**: 检查API服务是否正常运行  
**参数**: 无

**响应示例**:
```json
{
  "success": true,
  "message": "API is running"
}
```

---

#### 2. 用户登录

**接口**: `POST /api/users/login`  
**说明**: 用户登录验证  

**请求参数**:
```json
{
  "username": "admin",
  "password": "123456"
}
```

**响应示例**:
```json
{
  "success": true,
  "user": {
    "id": 1,
    "username": "admin",
    "name": "系统管理员",
    "role": "super_admin",
    "company_id": 1,
    "status": "enabled"
  }
}
```

---

#### 3. 获取用户列表

**接口**: `GET /api/users`  
**说明**: 获取所有启用状态的用户  

**响应示例**:
```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "username": "admin",
      "name": "系统管理员",
      "role": "super_admin",
      "company_id": 1,
      "status": "enabled",
      "created_at": "2026-02-01T10:00:00"
    }
  ]
}
```

---

#### 4. 添加用户

**接口**: `POST /api/users`  
**说明**: 新增用户  

**请求参数**:
```json
{
  "username": "zhangsan",
  "password": "123456",
  "name": "张三",
  "alias": "小张",
  "role": "operation",
  "company_id": 1
}
```

**响应示例**:
```json
{
  "success": true,
  "id": 42
}
```

---

#### 5. 更新用户

**接口**: `PUT /api/users/{user_id}`  
**说明**: 更新用户信息  

**请求参数**:
```json
{
  "name": "张三",
  "alias": "小张",
  "role": "financial",
  "company_id": 1,
  "status": "enabled"
}
```

**响应示例**:
```json
{
  "success": true
}
```

---

#### 6. 获取客户列表（分页）

**接口**: `GET /api/customers`  
**说明**: 获取客户列表，支持分页、搜索、筛选  

**请求参数**:
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | int | 否 | 页码，默认1 |
| page_size | int | 否 | 每页数量，默认20 |
| search | string | 否 | 搜索关键字（店铺名/商家ID/行业） |
| status | string | 否 | 状态筛选 |

**请求示例**:
```
GET /api/customers?page=1&page_size=20&search=餐厅&status=合作中
```

**响应示例**:
```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "merchant_id": "123456789",
      "shop_name": "XX餐厅",
      "industry": "餐饮",
      "status": "合作中",
      "follower_id": 5,
      "contacts": [
        {
          "name": "李经理",
          "phone": "13800138000",
          "position": "店长"
        }
      ],
      "memos": [
        {
          "date": "2026-02-01",
          "type": "沟通",
          "content": "初次接触，客户有意向"
        }
      ],
      "tags": ["重点客户", "餐饮"],
      "created_at": "2026-01-15T10:00:00"
    }
  ],
  "total": 186,
  "page": 1,
  "page_size": 20
}
```

---

#### 7. 获取客户详情

**接口**: `GET /api/customers/{customer_id}`  
**说明**: 获取单个客户的详细信息  

**响应示例**:
```json
{
  "success": true,
  "data": {
    "id": 1,
    "merchant_id": "123456789",
    "shop_name": "XX餐厅",
    "douyin_name": "XX餐厅官方",
    "company_name": "XX餐饮管理有限公司",
    "credit_code": "91410000MA45XXXX",
    "legal_person": "张三",
    "cooperation_mode": "短视频拍摄",
    "industry": "餐饮",
    "status": "合作中",
    "follower_id": 5,
    "business_staff": "张三",
    "service_staff": "李四",
    "operation_staff": "王五",
    "team": "商务一组",
    "region": "许昌",
    "project": "餐饮项目",
    "company": "许昌爱佳",
    "contacts": [...],
    "memos": [...],
    "tags": ["重点客户"],
    "created_at": "2026-01-15T10:00:00"
  }
}
```

---

#### 8. 添加客户

**接口**: `POST /api/customers`  
**说明**: 新增客户  

**请求参数**:
```json
{
  "merchant_id": "123456789",
  "shop_name": "XX餐厅",
  "douyin_name": "XX餐厅官方",
  "company_name": "XX餐饮管理有限公司",
  "credit_code": "91410000MA45XXXX",
  "legal_person": "张三",
  "cooperation_mode": "短视频拍摄",
  "industry": "餐饮",
  "status": "跟进中",
  "follower_id": 5,
  "business_staff": "张三",
  "service_staff": "李四",
  "operation_staff": "王五",
  "team": "商务一组",
  "region": "许昌",
  "project": "餐饮项目",
  "company": "许昌爱佳",
  "contacts": [
    {
      "name": "李经理",
      "phone": "13800138000",
      "position": "店长"
    }
  ],
  "memos": [
    {
      "date": "2026-02-01",
      "type": "沟通",
      "content": "初次接触"
    }
  ],
  "tags": ["重点客户"]
}
```

**响应示例**:
```json
{
  "success": true,
  "id": 7825
}
```

---

#### 9. 更新客户

**接口**: `PUT /api/customers/{customer_id}`  
**说明**: 更新客户信息  

**请求参数**: 同"添加客户"

**响应示例**:
```json
{
  "success": true
}
```

---

#### 10. 删除客户

**接口**: `DELETE /api/customers/{customer_id}`  
**说明**: 删除客户（级联删除联系人和备忘录）  

**响应示例**:
```json
{
  "success": true
}
```

---

#### 11. 获取订单列表

**接口**: `GET /api/orders`  
**说明**: 获取订单列表（支持分页、搜索、筛选）  

**请求参数**:
- `page`: 页码（默认1）
- `page_size`: 每页数量（默认20）
- `search`: 搜索关键词
- `status`: 订单状态
- `customer_id`: 客户ID

**响应示例**:
```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "order_number": "ORD202602010001",
      "customer_id": 5,
      "customer_name": "北京宏远科技",
      "order_date": "2026-02-01",
      "total_amount": 50000.00,
      "status": "服务中",
      "created_at": "2026-02-01T10:00:00"
    }
  ],
  "total": 50
}
```

---

#### 12. 添加订单

**接口**: `POST /api/orders`  
**说明**: 创建新订单（含订单明细）  

**请求参数**:
```json
{
  "customer_id": 5,
  "customer_name": "北京宏远科技",
  "order_date": "2026-02-01",
  "total_amount": 50000.00,
  "status": "待签约",
  "payment_method": "银行转账",
  "remarks": "订单备注",
  "created_by": "张伟",
  "items": [
    {
      "service_name": "短视频拍摄",
      "quantity": 10,
      "unit_price": 5000.00,
      "amount": 50000.00
    }
  ]
}
```

**响应示例**:
```json
{
  "success": true,
  "id": 1
}
```

---

#### 13. 获取订单详情

**接口**: `GET /api/orders/{order_id}`  
**说明**: 获取单个订单完整信息  

**响应示例**:
```json
{
  "success": true,
  "data": {
    "id": 1,
    "order_number": "ORD202602010001",
    "customer_id": 5,
    "total_amount": 50000.00,
    "items": [...],
    "payments": [...]
  }
}
```

---

#### 14. 更新订单

**接口**: `PUT /api/orders/{order_id}`  
**说明**: 更新订单信息  
**请求参数**: 同"添加订单"

---

#### 15. 删除订单

**接口**: `DELETE /api/orders/{order_id}`  
**说明**: 删除订单（软删除）  

---

#### 16. 获取财务流水列表

**接口**: `GET /api/transactions`  
**说明**: 获取财务流水记录（支持分页、搜索、筛选）  

**请求参数**:
- `page`: 页码
- `page_size`: 每页数量
- `type`: 类型（收入/支出）
- `start_date`: 开始日期
- `end_date`: 结束日期
- `search`: 搜索关键词

**响应示例**:
```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "transaction_type": "收入",
      "transaction_date": "2026-02-01",
      "amount": 5000.00,
      "payer_name": "客户A",
      "payee_name": "我司",
      "purpose": "订单收款"
    }
  ],
  "total": 100
}
```

---

#### 17. 添加流水记录

**接口**: `POST /api/transactions`  
**说明**: 创建财务流水记录  

**请求参数**:
```json
{
  "transaction_type": "收入",
  "transaction_date": "2026-02-01",
  "payer_name": "客户A",
  "payee_name": "我司",
  "amount": 5000.00,
  "purpose": "订单收款",
  "remark": "备注",
  "account_id": 1,
  "company_id": 1,
  "created_by": 1
}
```

---

#### 18. 批量添加流水

**接口**: `POST /api/transactions/batch`  
**说明**: 批量创建多条流水记录  

**请求参数**:
```json
{
  "transactions": [
    {
      "transaction_type": "收入",
      "transaction_date": "2026-02-01",
      "amount": 5000.00,
      ...
    },
    {...}
  ]
}
```

---

#### 19. 获取账户列表

**接口**: `GET /api/accounts`  
**说明**: 获取所有启用状态的账户  

**响应示例**:
```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "name": "公司基本账户",
      "bank_name": "中国工商银行",
      "account_number": "6222021234567890",
      "balance": 50000.00,
      "account_type": "银行账户",
      "status": "active"
    }
  ]
}
```

---

#### 20. 添加账户

**接口**: `POST /api/accounts`  
**说明**: 创建新账户  

**请求参数**:
```json
{
  "name": "支付宝账户",
  "bank_name": "支付宝",
  "account_number": "alipay@company.com",
  "balance": 0,
  "initial_balance": 0,
  "account_type": "电子账户",
  "status": "active"
}
```

---

#### 21. 获取公司列表

**接口**: `GET /api/companies`  
**说明**: 获取所有启用状态的公司  

**响应示例**:
```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "name": "北京宏远科技有限公司",
      "short_name": "宏远科技",
      "contact_person": "李明",
      "contact_phone": "13800138001",
      "status": "active"
    }
  ]
}
```

---

#### 22. 添加公司

**接口**: `POST /api/companies`  
**说明**: 创建新公司  

---

#### 23. 获取分类列表

**接口**: `GET /api/categories`  
**说明**: 获取收支分类列表  

**请求参数**:
- `type`: 分类类型（income-收入, expense-支出）

**响应示例**:
```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "name": "订单收款",
      "type": "income",
      "description": "客户订单收款",
      "sort_order": 1
    }
  ]
}
```

---

#### 24. 组织架构API

**部门管理**:
- `GET /api/departments` - 获取部门列表
- `POST /api/departments` - 添加部门
- `PUT /api/departments/{id}` - 更新部门
- `DELETE /api/departments/{id}` - 删除部门

**团队管理**:
- `GET /api/teams` - 获取团队列表
- `POST /api/teams` - 添加团队
- `PUT /api/teams/{id}` - 更新团队
- `DELETE /api/teams/{id}` - 删除团队

**岗位管理**:
- `GET /api/positions` - 获取岗位列表
- `POST /api/positions` - 添加岗位
- `PUT /api/positions/{id}` - 更新岗位
- `DELETE /api/positions/{id}` - 删除岗位

---

#### 25. 获取系统设置

**接口**: `GET /api/settings`  
**说明**: 获取系统配置项  

**响应示例**:
```json
{
  "success": true,
  "data": {
    "system_name": "财务流水账系统",
    "system_version": "4.6",
    "company_name": "许昌爱佳网络科技有限公司",
    "cooperation_modes": [
      "短视频拍摄",
      "直播代运营",
      "广告投放",
      "全案服务",
      "其他"
    ],
    "industries": [
      "餐饮",
      "零售",
      "美容美发",
      "汽车服务",
      "教育培训",
      "医疗健康",
      "休闲娱乐",
      "其他"
    ]
  }
}
```

---

## 部署与运维

### 服务器环境要求

- **操作系统**: Linux (推荐 Ubuntu 20.04+)
- **Web服务器**: Nginx 1.18+
- **数据库**: MySQL 8.0+
- **Python**: 3.8+
- **磁盘空间**: 至少 10GB
- **内存**: 至少 2GB

### 部署步骤

#### 1. 服务器准备

```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装必要软件
sudo apt install -y nginx mysql-server python3 python3-pip python3-venv git
```

#### 2. 配置MySQL数据库

```bash
# 登录MySQL
mysql -u root -p

# 创建数据库
CREATE DATABASE ajkuaiji DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 创建用户并授权
CREATE USER 'ajkuaiji'@'%' IDENTIFIED BY '@HNzb5z75b16';
GRANT ALL PRIVILEGES ON ajkuaiji.* TO 'ajkuaiji'@'%';
FLUSH PRIVILEGES;
EXIT;

# 导入表结构
mysql -h 47.98.60.197 -u ajkuaiji -p'@HNzb5z75b16' ajkuaiji < /root/ajkuaiji/backend/init_database.sql
```

#### 3. 部署后端API

```bash
# 进入后端目录
cd /root/ajkuaiji/backend

# 创建Python虚拟环境
python3 -m venv venv

# 激活虚拟环境
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt

# 启动API服务（使用Gunicorn）
nohup venv/bin/gunicorn -w 4 -b 0.0.0.0:5000 app:app > api.log 2>&1 &

# 查看服务状态
ps aux | grep gunicorn
```

**Gunicorn参数说明**:
- `-w 4`: 4个工作进程
- `-b 0.0.0.0:5000`: 监听5000端口
- `app:app`: 模块名:应用名

#### 4. 配置Nginx

```bash
# 编辑Nginx配置
sudo nano /etc/nginx/sites-available/ajkuaiji
```

**配置内容**:
```nginx
server {
    listen 80;
    server_name 47.98.60.197;
    
    # 前端静态文件
    root /root/ajkuaiji;
    index financial_system.html;
    
    # 前端路由
    location / {
        try_files $uri $uri/ /financial_system.html;
    }
    
    # 后端API代理
    location /api/ {
        proxy_pass http://127.0.0.1:5000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # CORS支持
        add_header Access-Control-Allow-Origin *;
        add_header Access-Control-Allow-Methods 'GET, POST, PUT, DELETE, OPTIONS';
        add_header Access-Control-Allow-Headers 'Content-Type, Authorization';
        
        if ($request_method = 'OPTIONS') {
            return 204;
        }
    }
    
    # 静态文件缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
    }
}
```

```bash
# 创建软链接
sudo ln -s /etc/nginx/sites-available/ajkuaiji /etc/nginx/sites-enabled/

# 测试配置
sudo nginx -t

# 重载Nginx
sudo nginx -s reload
```

#### 5. 配置系统服务（开机自启）

创建systemd服务文件:
```bash
sudo nano /etc/systemd/system/ajkuaiji-api.service
```

**服务配置**:
```ini
[Unit]
Description=AJKuaiJi API Service
After=network.target mysql.service

[Service]
Type=notify
User=root
WorkingDirectory=/root/ajkuaiji/backend
Environment="PATH=/root/ajkuaiji/backend/venv/bin"
ExecStart=/root/ajkuaiji/backend/venv/bin/gunicorn -w 4 -b 0.0.0.0:5000 app:app
ExecReload=/bin/kill -s HUP $MAINPID
KillMode=mixed
TimeoutStopSec=5
PrivateTmp=true
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
# 重载systemd
sudo systemctl daemon-reload

# 启动服务
sudo systemctl start ajkuaiji-api

# 设置开机自启
sudo systemctl enable ajkuaiji-api

# 查看服务状态
sudo systemctl status ajkuaiji-api
```

#### 6. 配置防火墙

```bash
# 开放80端口（HTTP）
sudo ufw allow 80/tcp

# 开放5000端口（API）
sudo ufw allow 5000/tcp

# 开放3306端口（MySQL）
sudo ufw allow 3306/tcp

# 启用防火墙
sudo ufw enable
```

### 数据迁移

将现有LocalStorage数据迁移到MySQL:

```bash
cd /root/ajkuaiji/backend

# 创建数据迁移脚本
cat > migrate_data.py << 'EOF'
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
import json
import pymysql
import sys

# 数据库配置
DB_CONFIG = {
    'host': '47.98.60.197',
    'user': 'ajkuaiji',
    'password': '@HNzb5z75b16',
    'database': 'ajkuaiji',
    'charset': 'utf8mb4'
}

# 从database.js提取数据
def extract_data_from_js():
    with open('../modules/database.js', 'r', encoding='utf-8') as f:
        content = f.read()
    
    # 找到getDefaultData函数中的users数组
    import re
    users_match = re.search(r'users: (\[.*?\]),', content, re.DOTALL)
    customers_match = re.search(r'customers: (\[.*?\]),', content, re.DOTALL)
    
    if not users_match or not customers_match:
        print("无法提取数据")
        sys.exit(1)
    
    users = json.loads(users_match.group(1))
    customers = json.loads(customers_match.group(1))
    
    return {'users': users, 'customers': customers}

# 迁移数据
def migrate():
    data = extract_data_from_js()
    conn = pymysql.connect(**DB_CONFIG)
    
    try:
        with conn.cursor() as cursor:
            # 迁移用户
            print(f"开始迁移 {len(data['users'])} 个用户...")
            for user in data['users']:
                sql = """INSERT INTO users (username, password, name, alias, role, company_id, status)
                         VALUES (%s, %s, %s, %s, %s, %s, %s)
                         ON DUPLICATE KEY UPDATE name=VALUES(name)"""
                cursor.execute(sql, (
                    user['username'], user['password'], user['name'],
                    user.get('alias'), user['role'], user.get('company_id'), 'enabled'
                ))
            
            # 迁移客户
            print(f"开始迁移 {len(data['customers'])} 个客户...")
            for customer in data['customers']:
                sql = """INSERT INTO customers (
                    merchant_id, shop_name, douyin_name, company_name, credit_code,
                    legal_person, cooperation_mode, industry, status, follower_id,
                    business_staff, service_staff, operation_staff, team, region, company
                ) VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)"""
                cursor.execute(sql, (
                    customer.get('merchant_id'), customer['shop_name'], customer.get('douyin_name'),
                    customer.get('company_name'), customer.get('credit_code'), customer.get('legal_person'),
                    customer.get('cooperation_mode'), customer.get('industry'), customer.get('status', '跟进中'),
                    customer.get('follower_id'), customer.get('business_staff'), customer.get('service_staff'),
                    customer.get('operation_staff'), customer.get('team'), customer.get('region'), customer.get('company')
                ))
            
            conn.commit()
            print("数据迁移完成！")
    finally:
        conn.close()

if __name__ == '__main__':
    migrate()
EOF

# 执行迁移
chmod +x migrate_data.py
venv/bin/python migrate_data.py
```

### 日常运维

#### 查看日志

```bash
# API日志
tail -f /root/ajkuaiji/backend/api.log

# Nginx访问日志
tail -f /var/log/nginx/access.log

# Nginx错误日志
tail -f /var/log/nginx/error.log

# 系统服务日志
journalctl -u ajkuaiji-api -f
```

#### 重启服务

```bash
# 重启API服务
sudo systemctl restart ajkuaiji-api

# 重启Nginx
sudo systemctl restart nginx

# 重启MySQL
sudo systemctl restart mysql
```

#### 数据库备份

```bash
# 手动备份
mysqldump -h 47.98.60.197 -u ajkuaiji -p'@HNzb5z75b16' ajkuaiji > backup_$(date +%Y%m%d).sql

# 创建自动备份脚本
cat > /root/backup_db.sh << 'EOF'
#!/bin/bash
BACKUP_DIR="/root/backups"
mkdir -p $BACKUP_DIR
DATE=$(date +%Y%m%d_%H%M%S)
mysqldump -h 47.98.60.197 -u ajkuaiji -p'@HNzb5z75b16' ajkuaiji | gzip > $BACKUP_DIR/ajkuaiji_$DATE.sql.gz
# 删除7天前的备份
find $BACKUP_DIR -name "ajkuaiji_*.sql.gz" -mtime +7 -delete
EOF

chmod +x /root/backup_db.sh

# 添加到crontab（每天凌晨2点备份）
echo "0 2 * * * /root/backup_db.sh" | crontab -
```

#### 数据库恢复

```bash
# 解压备份
gunzip backup_20260208.sql.gz

# 恢复数据
mysql -h 47.98.60.197 -u ajkuaiji -p'@HNzb5z75b16' ajkuaiji < backup_20260208.sql
```

#### 性能监控

```bash
# 查看API进程资源占用
ps aux | grep gunicorn

# 查看MySQL连接数
mysql -h 47.98.60.197 -u ajkuaiji -p'@HNzb5z75b16' -e "SHOW STATUS LIKE 'Threads_connected';"

# 查看数据库大小
mysql -h 47.98.60.197 -u ajkuaiji -p'@HNzb5z75b16' -e "SELECT table_schema AS 'Database', ROUND(SUM(data_length + index_length) / 1024 / 1024, 2) AS 'Size (MB)' FROM information_schema.tables WHERE table_schema = 'ajkuaiji' GROUP BY table_schema;"
```

---

## 使用指南

### 首次登录

1. **访问系统**
   - 打开浏览器，访问: `http://47.98.60.197`
   
2. **使用默认管理员账户登录**
   - 用户名: `admin`
   - 密码: `123456`

3. **登录后系统自动跳转到仪表盘**

### 客户管理操作流程

#### 新增客户

1. 点击左侧菜单 **"客户"**
2. 点击右上角 **"+ 新增客户"** 按钮
3. 填写客户信息（必填项：店铺名称）
   - **基本信息**面板: 商家ID、店铺名称、抖音号等
   - **企业信息**面板: 公司名称、法人、注册资本等
   - **合作信息**面板: 合作模式、行业、状态等
   - **人员分配**面板: 跟进人、商务、服务、运营人员等
   - **联系人**面板: 点击"添加联系人"，填写姓名、电话、职位
   - **备忘录**面板: 点击"添加备忘录"，记录沟通内容
4. 点击 **"保存"** 按钮

#### 搜索客户

1. 在客户列表页顶部搜索框输入关键字
   - 支持按店铺名称、商家ID、行业搜索
2. 系统自动执行搜索（300ms防抖）

#### 筛选客户

1. 点击状态下拉框
2. 选择状态:
   - 全部状态
   - 合作中
   - 跟进中
   - 已流失
3. 列表自动刷新

#### 编辑客户

1. 在客户列表中点击客户行的 **"编辑"** 按钮
2. 修改客户信息
3. 点击 **"保存"** 按钮

#### 删除客户

1. 在客户列表中点击客户行的 **"删除"** 按钮
2. 确认删除操作
3. 客户及其关联的联系人、备忘录将被删除

#### 分页操作

1. 客户列表底部显示分页控件
2. **切换页码**: 点击页码按钮
3. **调整每页数量**: 选择20/50/100/500/全部
4. 分页信息实时更新

### 订单管理操作流程

#### 创建订单

1. 点击左侧菜单 **"订单"**
2. 点击 **"+ 新增订单"** 按钮
3. 填写订单信息:
   - **订单日期**: 默认今天，可修改
   - **选择客户**: 从下拉列表选择（如未找到需先在客户管理中添加）
   - **人员配置**: 选择商务、服务、运营、管理人员
   - **组织信息**: 选择团队、地区、项目、公司
4. **添加商品/服务项**:
   - 点击 **"+ 添加服务项"** 按钮
   - 选择服务名称（如：短视频拍摄）
   - 系统自动填充服务类型（如：基础套餐）
   - 输入单价和数量
   - 系统自动计算小计和订单总金额
5. **填写备注**: 输入订单备注信息（可多条）
6. **上传附件**: 点击 **"选择文件"** 上传合同、方案等文件
7. 点击 **"保存"** 按钮

#### 记录收款

1. 在订单列表中找到目标订单
2. 点击 **"收款"** 按钮
3. 填写收款信息:
   - 收款日期
   - 收款金额
   - 收款方式（现金/转账/支付宝/微信等）
   - 备注说明
4. 点击 **"保存"** 按钮
5. 系统自动更新订单已收款金额和未收款金额

#### 添加售后记录

1. 在订单列表中找到目标订单
2. 点击 **"售后"** 按钮
3. 填写售后信息:
   - 售后时间
   - 售后类型（退款/重做/修改等）
   - 问题描述
   - 处理结果
4. 点击 **"保存"** 按钮

### 任务池操作流程

#### 查看任务

1. 点击左侧菜单 **"任务池"**
2. 页面顶部显示任务统计卡片:
   - 待接单数量
   - 进行中数量
   - 已完成数量
3. 点击筛选按钮:
   - **全部**: 显示所有任务
   - **待接单**: 只显示待接单任务
   - **进行中**: 只显示进行中任务
   - **已完成**: 只显示已完成任务

#### 接单操作

1. 在任务池列表中找到 **"待接单"** 状态的任务
2. 点击 **"接单"** 按钮
3. 系统自动:
   - 将任务状态更新为 **"进行中"**
   - 将当前登录用户设置为任务执行人
   - 记录接单时间

#### 登记任务成本

1. 在任务池列表中找到 **"进行中"** 状态的任务
2. 点击 **"登记成本"** 按钮
3. 在弹窗中填写成本信息:
   - **成本类别**: 选择（拍摄费/投放费/人员成本/设备租赁/其他）
   - **金额**: 输入成本金额
   - **备注**: 说明成本明细
4. 点击 **"保存"** 按钮
5. 可以多次登记成本，系统自动累加总成本

#### 完成任务

1. 任务执行完毕后，点击 **"完成"** 按钮
2. 确认完成操作
3. 系统自动:
   - 将任务状态更新为 **"已完成"**
   - 记录完成时间
   - 计算任务利润 = 订单金额 - 总成本

#### 查看任务成本明细

1. 在任务池列表中找到 **"已完成"** 状态的任务
2. 点击 **"查看成本"** 按钮
3. 弹窗显示该任务的所有成本记录

### 财务流水操作流程

#### 手动录入流水

1. 点击左侧菜单 **"流水"**
2. 点击 **"+ 新增流水"** 按钮
3. 填写流水信息:
   - **交易日期**: 选择日期
   - **交易类型**: 选择来账（收入）或往账（支出）
   - **付款方银行**: 输入银行名称
   - **付款方名称**: 输入付款单位/个人
   - **收款方银行**: 输入银行名称
   - **收款方名称**: 输入收款单位
   - **金额**: 输入金额（来账为正数，往账为负数）
   - **交易后余额**: 输入账户余额
   - **用途**: 选择流水用途类别
   - **备注**: 填写补充说明
4. 点击 **"保存"** 按钮

#### 流水分类管理

1. 点击左侧菜单 **"流水分类"**
2. 查看现有分类:
   - **收入类**: 显示绿色
   - **支出类**: 显示红色
   - **收支两用**: 显示灰色
3. 新增分类:
   - 点击 **"+ 新增分类"** 按钮
   - 填写分类名称和类型
   - 点击 **"保存"**

#### 流水审核

1. 在流水列表中找到 **"待审核"** 状态的流水
2. 点击 **"审核"** 按钮
3. 选择审核结果:
   - **通过**: 流水状态更新为"已审核"
   - **驳回**: 流水状态更新为"已驳回"

#### 流水作废

1. 在流水列表中找到需要作废的流水
2. 点击 **"作废"** 按钮
3. 确认作废操作
4. 流水标记为作废，不再参与统计

### 组织架构管理

#### 添加人员

1. 点击左侧菜单 **"组织架构"**
2. 点击 **"+ 新增人员"** 按钮
3. 填写人员信息:
   - **登录用户名**: 唯一，用于登录（必填）
   - **登录密码**: 至少6位（必填）
   - **真实姓名**: 人员姓名（必填）
   - **别名/花名**: 可选
   - **角色**: 选择权限角色
     - super_admin: 超级管理员
     - admin: 管理员
     - financial: 财务人员
     - operation: 运营人员
     - viewer: 只读用户
   - **所属公司**: 选择公司
4. 点击 **"保存"** 按钮

#### 编辑人员

1. 在人员列表中点击 **"编辑"** 按钮
2. 修改人员信息
3. 点击 **"保存"** 按钮

#### 删除人员

1. 在人员列表中点击 **"删除"** 按钮
2. 确认删除操作
3. 人员账户被删除，无法再登录系统

**注意**: 删除人员不会删除该人员关联的历史数据（如跟进的客户、创建的订单等）

### 系统设置

#### 基础设置

1. 点击左侧菜单 **"设置"**
2. 在 **"基础设置"** 标签页:
   - 修改系统名称
   - 修改公司信息
3. 点击 **"保存"** 按钮

#### 数据字典管理

1. 在 **"数据字典"** 标签页
2. **合作模式**: 添加/删除/编辑合作模式选项
3. **行业分类**: 添加/删除/编辑行业选项
4. **服务项目**: 添加/删除/编辑服务项目
5. 点击 **"保存"** 按钮

#### 数据导入

1. 在 **"数据管理"** 标签页
2. 点击 **"选择文件"** 按钮
3. 选择CSV格式的数据文件
4. 点击 **"导入"** 按钮
5. 系统自动解析并导入数据

#### 数据导出

1. 在 **"数据管理"** 标签页
2. 选择导出范围:
   - 全部数据
   - 客户数据
   - 订单数据
   - 流水数据
3. 点击 **"导出"** 按钮
4. 系统生成Excel文件并下载

---

## 常见问题

### Q1: 登录后页面空白或无法加载？

**解决方案**:
1. 清除浏览器缓存（按 Ctrl+Shift+Delete）
2. 强制刷新页面（按 Ctrl+F5）
3. 检查浏览器控制台是否有JavaScript错误
4. 确认网络连接正常
5. 检查API服务是否正常运行: `curl http://localhost:5000/api/health`

### Q2: 无法添加客户，提示"商家ID已存在"？

**原因**: 系统中已存在相同商家ID的客户

**解决方案**:
1. 在客户列表搜索框输入该商家ID
2. 找到已存在的客户记录
3. 选择:
   - 编辑现有客户信息
   - 或删除旧记录后重新添加
   - 或为新客户使用不同的商家ID

### Q3: 分页功能不工作？

**解决方案**:
1. 检查浏览器控制台是否有错误
2. 确认数据量是否超过当前页大小（如数据只有10条，每页20条则不会显示分页）
3. 刷新页面重新加载
4. 检查customers.js版本是否为4.6+（查看HTML中script标签的v参数）

### Q4: 数据保存后刷新页面丢失？

**原因**: 旧版本使用LocalStorage存储，清除浏览器数据会导致丢失

**解决方案**（v4.6+已解决）:
1. 系统已升级为MySQL数据库，数据永久存储
2. 如仍使用旧版本，请联系管理员升级系统
3. 升级前请勿清除浏览器缓存

### Q5: API请求失败，返回500错误？

**排查步骤**:
1. 检查API服务状态:
   ```bash
   sudo systemctl status ajkuaiji-api
   ps aux | grep gunicorn
   ```

2. 查看API日志:
   ```bash
   tail -f /root/ajkuaiji/backend/api.log
   ```

3. 检查数据库连接:
   ```bash
   mysql -h 47.98.60.197 -u ajkuaiji -p'@HNzb5z75b16' ajkuaiji -e "SELECT 1;"
   ```

4. 重启API服务:
   ```bash
   sudo systemctl restart ajkuaiji-api
   ```

### Q6: 无法接单，提示权限不足？

**原因**: 当前登录用户角色权限不够

**解决方案**:
1. 确认当前用户角色:
   - viewer角色无法操作数据
   - operation角色才能接单
2. 联系管理员修改用户角色
3. 或使用具有相应权限的账户登录

### Q7: 财务流水统计数据不准确？

**排查方法**:
1. 检查是否有流水被标记为"作废"
   - 作废的流水不参与统计
2. 检查流水的交易类型是否正确
   - 来账应为正数
   - 往账应为负数
3. 检查日期筛选条件
4. 检查是否选择了正确的账户和公司

### Q8: 上传附件失败？

**可能原因**:
1. 文件大小超过限制（默认10MB）
2. 文件类型不支持
3. 服务器磁盘空间不足

**解决方案**:
1. 压缩文件后再上传
2. 检查文件格式是否为支持的类型（pdf, doc, docx, jpg, png等）
3. 联系管理员检查服务器空间:
   ```bash
   df -h
   ```

### Q9: 系统运行缓慢？

**优化建议**:
1. 清理浏览器缓存和Cookie
2. 关闭不必要的浏览器标签页
3. 使用Chrome或Firefox最新版本浏览器
4. 检查网络连接速度
5. 联系管理员优化服务器性能:
   - 增加Gunicorn工作进程数
   - 优化MySQL查询索引
   - 升级服务器配置

### Q10: 忘记登录密码怎么办？

**解决方案**:
1. 联系管理员重置密码
2. 管理员可通过以下方式重置:
   ```bash
   # 登录MySQL
   mysql -h 47.98.60.197 -u ajkuaiji -p'@HNzb5z75b16' ajkuaiji
   
   # 重置密码（将zhangsan的密码重置为123456）
   UPDATE users SET password='123456' WHERE username='zhangsan';
   ```

### Q11: 导出数据时提示错误？

**解决方案**:
1. 检查数据量是否过大
   - 建议分批导出（按日期范围）
2. 检查浏览器是否阻止下载
   - 允许浏览器下载权限
3. 检查服务器临时目录空间
   ```bash
   df -h /tmp
   ```

### Q12: 多人同时操作会冲突吗？

**回答**: 
v4.6+版本使用MySQL数据库，支持多用户并发操作，不会出现数据冲突。数据库自动处理并发访问和数据一致性。

但需注意:
- 同时编辑同一条记录时，后提交的会覆盖先提交的
- 建议通过分配明确的负责人避免冲突

---

## 系统运行逻辑总结

### 数据流转图

```
用户登录
    ↓
权限验证（database.js/API）
    ↓
加载用户数据（LocalStorage/MySQL）
    ↓
初始化模块（core.js调用各模块init函数）
    ↓
用户操作（增删改查）
    ↓
前端JavaScript处理（各模块.js）
    ↓
v4.5及之前: 保存到LocalStorage
v4.6及之后: 调用API → MySQL数据库
    ↓
返回结果，更新界面
    ↓
显示Toast提示
```

### 业务运转核心逻辑

1. **客户获取阶段**
   - 市场/商务人员在"客户管理"新增客户
   - 状态设为"跟进中"
   - 分配跟进人

2. **客户跟进阶段**
   - 跟进人定期添加备忘录记录沟通进展
   - 更新客户状态（跟进中→合作中）

3. **签约阶段**
   - 在"订单管理"创建订单
   - 关联客户、填写合同金额、添加服务项
   - 配置执行团队人员

4. **任务执行阶段**
   - 订单自动生成任务到"任务池"
   - 运营人员接单
   - 执行过程中登记各项成本
   - 完成后标记任务完成

5. **财务结算阶段**
   - 客户付款后在"财务流水"录入收款记录
   - 或在订单中添加收款记录
   - 系统自动更新账户余额

6. **数据分析阶段**
   - 在"仪表盘"查看整体经营数据
   - 在"报表"模块生成各类统计报表
   - 导出数据进行深度分析

### 权限控制逻辑

| 角色 | 客户管理 | 订单管理 | 任务池 | 财务流水 | 组织架构 | 系统设置 |
|------|---------|---------|--------|---------|---------|----------|
| super_admin | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| admin | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| financial | ✅ | ✅ | ❌ | ✅ | ❌ | ❌ |
| operation | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ |
| viewer | 👁️ | 👁️ | 👁️ | 👁️ | ❌ | ❌ |

**说明**:
- ✅ 完全访问（增删改查）
- 👁️ 只读访问（仅查看）
- ❌ 无权访问

---

---

## SaaS超级控制台规划

### 产品愿景

将本系统打造成**可销售的SaaS产品**，通过超级控制台实现多企业客户的统一管理、服务开通、计费结算等功能，形成可规模化商业运营的ERP SaaS平台。

### 超级控制台功能规划

#### 1. 企业客户管理(Companies Management)

**1.1 客户开通**
- 新增企业客户
- 配置企业基本信息:
  - 企业名称、简称
  - 统一社会信用代码
  - 联系人、联系方式
  - 企业规模、行业类型
- 分配独立数据库空间(多租户隔离)
- 生成企业专属访问域名/子域名
- 初始化企业管理员账户

**1.2 服务套餐管理**
- 套餐定义:
  - 基础版(5用户、1000客户、10GB存储)
  - 专业版(20用户、5000客户、50GB存储)
  - 企业版(无限用户、无限客户、200GB存储)
- 功能模块开关:
  - 客户管理(基础功能)
  - 订单管理
  - 任务池管理
  - 财务流水
  - 高级报表
  - API接口
- 服务期限设置(月付/年付)

**1.3 客户状态监控**
- 服务状态:
  - 试用中(7-30天)
  - 正常服务
  - 欠费停服
  - 已到期
  - 已注销
- 使用情况统计:
  - 用户数使用率
  - 数据存储使用率
  - API调用量
  - 活跃度指标(日活/月活)
- 到期预警:
  - 服务到期前30/15/7/1天自动提醒
  - 欠费停服流程

#### 2. 计费结算管理(Billing Management)

**2.1 费用计算**
- 基础服务费(按套餐)
- 增值服务费:
  - 超出套餐的用户数(每个/月)
  - 超出套餐的存储空间(每GB/月)
  - API调用超额费用
  - 定制开发费用
- 折扣管理:
  - 年付折扣(如:9折)
  - 批量采购折扣
  - 推广期优惠

**2.2 账单管理**
- 自动生成月度账单
- 账单明细:
  - 基础服务费
  - 增值服务费
  - 折扣金额
  - 应付总额
- 账单状态:
  - 未出账
  - 待支付
  - 已支付
  - 已逾期
- 发票管理:
  - 电子发票生成
  - 纸质发票申请

**2.3 收款管理**
- 支持多种支付方式:
  - 银行转账
  - 支付宝/微信
  - 对公账户
- 自动到账确认
- 收款记录查询
- 财务对账功能

#### 3. 系统运维管理(System Operations)

**3.1 数据备份与恢复**
- 自动备份策略:
  - 每日增量备份
  - 每周全量备份
  - 备份保留30天
- 数据恢复:
  - 按企业恢复
  - 按时间点恢复
  - 紧急回滚机制

**3.2 系统监控**
- 服务健康监控:
  - API响应时间
  - 数据库连接池状态
  - 服务器CPU/内存/磁盘
- 异常告警:
  - 服务宕机告警
  - 性能下降告警
  - 磁盘空间不足告警
- 日志管理:
  - 访问日志
  - 错误日志
  - 操作审计日志

**3.3 版本更新管理**
- 灰度发布:
  - 选择部分企业先行体验新版本
  - 观察稳定性后全量发布
- 版本回滚:
  - 发现问题快速回滚到上一版本
- 更新通知:
  - 向企业客户推送更新说明
  - 重大功能变更培训

#### 4. 数据分析与报表(Analytics)

**4.1 平台整体数据**
- 企业客户数:
  - 总客户数
  - 新增客户趋势
  - 流失客户分析
- 收入统计:
  - 月度/年度收入
  - 套餐收入占比
  - 增值服务收入占比
- 用户活跃度:
  - 日活跃企业数
  - 月活跃企业数
  - 用户留存率

**4.2 企业客户画像**
- 行业分布
- 企业规模分布
- 地域分布
- 套餐选择偏好
- 功能使用热度

**4.3 运营报表**
- 销售漏斗:
  - 试用申请数
  - 试用转化率
  - 续费率
  - 流失率
- 客户生命周期价值(LTV)
- 客户获取成本(CAC)

#### 5. 权限与安全(Security)

**5.1 平台管理员角色**
- 超级管理员:
  - 全部权限
  - 企业数据访问(仅用于技术支持)
- 销售人员:
  - 企业开通
  - 套餐变更
  - 查看企业信息
- 财务人员:
  - 账单管理
  - 收款确认
  - 财务报表
- 运维人员:
  - 系统监控
  - 数据备份
  - 故障处理

**5.2 数据安全**
- 多租户数据隔离:
  - 数据库级别隔离(每个企业独立数据库)
  - 或表级别隔离(共享数据库,按company_id区分)
- 数据加密:
  - 敏感数据加密存储
  - 传输层TLS/SSL加密
- 访问审计:
  - 记录所有平台管理员操作
  - 企业数据访问日志

**5.3 合规管理**
- 数据导出:
  - 企业申请导出自有数据
- 数据删除:
  - 企业注销后数据保留策略
  - 符合GDPR等法规要求
- 服务协议:
  - SLA服务等级协议
  - 数据隐私协议

### 技术架构升级规划

#### 多租户架构方案

**方案A: 独立数据库(推荐)**
```
企业A → ajkuaiji_company_001 (MySQL Database)
企业B → ajkuaiji_company_002 (MySQL Database)
企业C → ajkuaiji_company_003 (MySQL Database)
```
**优点**:
- 数据完全隔离,安全性高
- 单个企业故障不影响其他企业
- 便于数据迁移和备份

**缺点**:
- 数据库实例多,运维成本高
- 需要连接池管理

**方案B: 共享数据库+company_id区分**
```
users表:
  id | company_id | username | ...
  1  | 1          | admin    | ...
  2  | 2          | admin    | ...
  
customers表:
  id | company_id | shop_name | ...
  1  | 1          | XX餐厅    | ...
  2  | 2          | YY商店    | ...
```
**优点**:
- 运维简单,只需管理一个数据库
- 成本低

**缺点**:
- 数据隔离依赖应用层逻辑
- 需要严格的权限控制
- 单点故障影响所有企业

#### 访问域名方案

**子域名方案**:
```
https://company-a.ajkuaiji.com → 企业A系统
https://company-b.ajkuaiji.com → 企业B系统
https://console.ajkuaiji.com   → 超级控制台
```

**路径方案**:
```
https://ajkuaiji.com/company-a → 企业A系统
https://ajkuaiji.com/company-b → 企业B系统
https://ajkuaiji.com/console   → 超级控制台
```

### 开发路线图(Roadmap)

#### Phase 1: 基础设施准备(2周)
- [ ] 设计多租户数据库架构
- [ ] 创建companies_meta表(平台级企业客户表)
- [ ] 实现企业客户CRUD接口
- [ ] console.html超级控制台登录功能
- [ ] 平台管理员权限体系

#### Phase 2: 企业管理功能(3周)
- [ ] 企业客户开通流程
- [ ] 套餐管理功能
- [ ] 服务状态监控
- [ ] 企业数据初始化脚本
- [ ] 企业访问域名绑定

#### Phase 3: 计费结算功能(3周)
- [ ] 套餐定价配置
- [ ] 自动账单生成
- [ ] 支付接口对接
- [ ] 收款确认流程
- [ ] 欠费停服机制

#### Phase 4: 运维监控功能(2周)
- [ ] 系统监控面板
- [ ] 自动备份脚本
- [ ] 数据恢复工具
- [ ] 日志查询界面
- [ ] 告警通知系统

#### Phase 5: 数据分析功能(2周)
- [ ] 平台整体数据看板
- [ ] 企业客户画像
- [ ] 运营报表生成
- [ ] 数据导出功能

#### Phase 6: 商业化准备(1周)
- [ ] 官网介绍页面
- [ ] 试用申请流程
- [ ] 在线购买流程
- [ ] 帮助文档中心
- [ ] 客户案例展示

### 商业模式

#### 定价策略

| 套餐 | 基础版 | 专业版 | 企业版 |
|------|--------|--------|--------|
| 用户数 | 5 | 20 | 无限 |
| 客户数 | 1000 | 5000 | 无限 |
| 存储空间 | 10GB | 50GB | 200GB |
| 功能模块 | 基础 | 全部 | 全部+定制 |
| 技术支持 | 工单 | 工单+电话 | 7x24专属 |
| 价格(年付) | ¥6,000/年 | ¥18,000/年 | ¥50,000/年 |
| 价格(月付) | ¥600/月 | ¥1,800/月 | ¥5,000/月 |

#### 目标客户

- **基础版**: 10人以下小微企业、个体工商户
- **专业版**: 10-50人中小企业、连锁门店
- **企业版**: 50人以上中大型企业、集团公司

#### 推广策略

1. **免费试用**: 14天全功能试用
2. **首年优惠**: 首年7折优惠
3. **推荐返利**: 老客户推荐新客户,双方各获1个月免费使用
4. **行业定制**: 针对特定行业(如餐饮、零售)推出行业版

### 当前状态

**已完成**:
- ✅ 主系统(`financial_system.html`)核心功能开发完成(v14.0)
- ✅ 超级控制台(`console.html`)基础框架已搭建
- ✅ 单租户模式运行稳定

**待开发**:
- ⏳ 多租户架构改造
- ⏳ 企业客户管理功能
- ⏳ 计费结算功能
- ⏳ 运维监控功能
- ⏳ 数据分析功能

**注意事项**:
- 超级控制台功能目前处于规划阶段,尚未完全实现
- 现有console.html仅实现了登录框架和基础布局
- 后续开发需要按照上述路线图逐步推进
- 商业化前需要完成安全评估和压力测试

---

## 附录

### A. 文件结构

```
/root/ajkuaiji/
├── financial_system.html          # 主HTML文件
├── modules/                       # 功能模块目录
│   ├── core.js                   # 核心初始化模块
│   ├── database.js               # 数据库模块（2.5MB，包含默认数据）
│   ├── login.js                  # 登录模块
│   ├── navigation.js             # 导航模块
│   ├── dashboard.js              # 仪表盘模块
│   ├── customers.js              # 客户管理模块
│   ├── orders.js                 # 订单管理模块
│   ├── taskpool.js               # 任务池模块
│   ├── transactions.js           # 财务流水模块
│   ├── categories.js             # 流水分类模块
│   ├── organization.js           # 组织架构模块
│   ├── settings.js               # 系统设置模块
│   ├── reports.js                # 报表模块
│   ├── services.js               # 服务项管理模块
│   └── inventory.js              # 库存管理模块（预留）
├── backend/                       # 后端目录（v4.6新增）
│   ├── app.py                    # Flask主应用
│   ├── requirements.txt          # Python依赖
│   ├── init_database.sql         # 数据库初始化SQL
│   ├── venv/                     # Python虚拟环境
│   └── api.log                   # API日志
├── shangjaimingxi.csv            # 原始商家数据（2.4MB）
└── 系统说明文档.md                # 本文档
```

### B. 关键配置参数

**数据库连接**:
```python
DB_CONFIG = {
    'host': '47.98.60.197',
    'user': 'ajkuaiji',
    'password': '@HNzb5z75b16',
    'database': 'ajkuaiji',
    'charset': 'utf8mb4'
}
```

**API服务**:
- 监听地址: 0.0.0.0:5000
- 工作进程: 4个
- WSGI服务器: Gunicorn

**Nginx配置**:
- 前端端口: 80
- API代理: /api/ → http://127.0.0.1:5000/api/

**分页默认值**:
- 每页显示: 20条
- 可选项: 20/50/100/500/全部

**搜索防抖**: 300ms

### C. 版本更新历史

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| v1.0 | 2024-12 | 初始版本，基础功能 |
| v2.0 | 2025-06 | 增加订单管理、任务池 |
| v3.0 | 2025-12 | 优化UI，增加权限控制 |
| v4.0 | 2026-01 | 增加报表模块 |
| v4.5 | 2026-02-05 | 客户管理分页功能 |
| v4.6 | 2026-02-08 | **架构升级：LocalStorage → MySQL数据库** |
| v14.1 | 2026-02-11 | 完善开发文档、验证功能实现、同步代码与文档 |

### D. 技术支持

**开发团队**: 许昌爱佳网络科技有限公司  
**技术支持**: 请联系系统管理员  
**系统维护**: 24小时监控，工作日响应

---

## 结语

本系统经过多个版本迭代，从最初的本地存储演进到现在的数据库架构，功能日趋完善。v4.6版本的架构升级是一个重要里程碑，为系统的可扩展性和数据安全性奠定了坚实基础。

希望本文档能帮助您快速了解和使用系统。如有任何问题或建议，欢迎反馈！

---

**文档版本**: 1.0  
**最后更新**: 2026年2月8日  
**文档状态**: ✅ 完整版